name: Pr3 A1

on:
  push:
    #Ejecutar el ci cuando se haga push en estas ramas
    branches: [develop]
  pull_request:
    #Ejecutar el ci en master cuando se haga PR en estas ramas
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies (lockfile)
        run: npm ci

      - name: Run tests
        run: npm test